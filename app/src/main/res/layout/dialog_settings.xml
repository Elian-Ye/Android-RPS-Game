<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_settings_popup"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:includeFontPadding="false"
        android:text="@string/settings"
        android:textColor="@color/rps_text_primary"
        android:textSize="14sp"
        android:textStyle="bold" />

    <androidx.appcompat.widget.SwitchCompat
        android:id="@+id/switchDarkMode"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_marginTop="8dp"
        android:gravity="center_vertical"
        android:text="@string/dark_mode"
        android:textColor="@color/rps_text_primary"
        android:textSize="15sp"
        app:thumbTint="@color/rps_switch_thumb"
        app:trackTint="@color/rps_switch_track" />

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginVertical="8dp"
        android:background="@color/rps_border" />

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/btnResetRecord"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:background="?attr/selectableItemBackground"
        android:gravity="start|center_vertical"
        android:minWidth="0dp"
        android:minHeight="0dp"
        android:paddingHorizontal="0dp"
        android:text="@string/reset_record"
        android:textAllCaps="false"
        android:textColor="@color/rps_result_lose"
        android:textSize="13sp" />
</LinearLayout>
